services:
  caddy:
    profiles: [direct]

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${TUNNEL_TOKEN:?TUNNEL_TOKEN in .env setzen}
    depends_on:
      app:
        condition: service_healthy
